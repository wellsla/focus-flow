# Local development environment variables for FocusFlow
# Copy this file to `.env.local` and fill in the values.
# Never commit your real secrets.

# --- App URLs ---
# Canonical base URL of your app (used by Auth0 SDK for redirects and cookies)
APP_BASE_URL=http://localhost:3000
# Optional: used by sitemap or other places; keep equal to APP_BASE_URL
AUTH0_BASE_URL=http://localhost:3000
# If you proxy to a separate backend for certain routes
BACKEND_ORIGIN=http://localhost:9003

# --- Auth0 (required) ---
# Find these in Auth0 Dashboard > Applications > Your App > Settings
AUTH0_DOMAIN=wellsla.us.auth0.com
AUTH0_CLIENT_ID=replace-with-your-client-id
AUTH0_CLIENT_SECRET=replace-with-your-client-secret
# A long, random secret used to encrypt cookies/session (32+ chars)
# You can generate with Node: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH0_SECRET=replace-with-a-long-random-string

# --- Database (Prisma) ---
# Example for Vercel Postgres or your own Postgres connection string
DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require

# --- Next.js / Client feature flags ---
# Toggle optional UI features; "false" to disable
NEXT_PUBLIC_ENABLE_MOTIVATIONAL_HEADER=true
# Enable React Compiler only in dev by default; set to "off" to disable in dev
NEXT_PUBLIC_REACT_COMPILER=on

# --- Misc (optional) ---
# Explicit port when running locally
# PORT=3000
# Vercel sets this automatically in production; do not set manually
# VERCEL_URL=
